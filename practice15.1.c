//二进制转换为整数
# include <stdio.h>
void binary(int n);
int main(void)
{
char *pbin="01001001";//作为一个字符串
binary(*pbin);
printf("%d", *pbin);
return 0;
}
void binary(int n)
{
    int i,index;
    char *pbin[i];
    if (*pbin[i] ==1?1:0)
        for(i=0;i<index;i++)
        *pbin[i]+=1*(2*i);
        return(*pbin);
}
